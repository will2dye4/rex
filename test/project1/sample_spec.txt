%%$DIGIT [0-9]
%%$NON-ZERO [^0] IN $DIGIT
%%$CHAR [a-zA-Z]
%%$UPPER [^a-z] IN $CHAR
%%$LOWER [^A-Z] IN $CHAR
$LETTER  [a-zA-Z]

$FOO     arg (a|b|c|d|e|f|g)*

%%$IDENTIFIER $LOWER ($LOWER|$DIGIT)*
%%$INT ($DIGIT)+
%%$FLOAT ($DIGIT)+ \. ($DIGIT)+
%%$ASSIGN =
%%$PLUS \+
%%$MINUS -
%%$MULTIPLY \*
%%$PRINT PRI NT       %% Works fine like this, just not as "PRINT"...
